# Python bytecode and caches
__pycache__/
*.pyc
.pytest_cache/

# Local env files (already tracked .env variants remain)
.env.local

# SQLite dev DB (uncomment to ignore if not tracking)
# db.sqlite3
